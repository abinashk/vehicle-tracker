name: shared
description: Shared models, enums, constants, and utilities for the Vehicle Tracker system.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  uuid: ^4.2.1
  intl: ^0.19.0

dev_dependencies:
  test: ^1.25.0
  flutter_lints: ^3.0.0
