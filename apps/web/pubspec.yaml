name: vehicle_tracker_web
description: Admin web dashboard for vehicle speed monitoring system management.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  shared:
    path: ../../packages/shared
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  supabase_flutter: ^2.6.0
  go_router: ^14.2.0
  fl_chart: ^0.68.0
  csv: ^6.0.0
  intl: ^0.19.0
  data_table_2: ^2.5.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.0
  mockito: ^5.4.4

flutter:
  uses-material-design: true
